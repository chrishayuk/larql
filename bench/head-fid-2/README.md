# HEAD-FID-2 evidence

The freeze, rule and adjudication are in `docs/head-fid-2.md`. The verdict is **ACCEPTABLE**.

- `run.sh`: the frozen invocation, run as `run.sh a7f69949`.
- `meta.txt`: binary identity, checkout, `LARQL_*` env, and load per run.
- `r-to-c/`, `r-to-h/`, `c-to-h/`: each measurement's report, receipt and per-position records. Each has a
  matching `.txt` summary. The home directory is rewritten to `~`.
