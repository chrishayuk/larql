{
  "programme": "CONTINUATION-VIEW-1",
  "forecast": "continuation-view-1.json (frozen at main 8b911f20, #552; not edited)",
  "rule": "The execution record. The forecast is not edited; a wave that finds the design must change writes the change and the reason here.",
  "waves": {
    "V1": {
      "name": "one retention authority",
      "status": "built and held",
      "base": "main 5d09737e (rebased from 8b911f20 over #553, #554 and the docs-only #555; no conflict)",
      "design": {
        "policy": "`HistoryRange { Full, Trailing(usize) }` in exec/kv.rs beside LayerKvGeometry, with `HistoryRange::of_span(span, window)` — ONE exhaustive match over AttentionSpan, the only place the policy is written — and `required_range(position) -> Range<usize>` (= required_start(position)..position + 1).",
        "mapping": "Sliding + Some(w) → Trailing(w); Sliding + None → Full (no declared bound to apply); Full → Full; Windowed → Full (its extent is spatial, not a position count — the AttentionSpan::Windowed doc already forbids inferring dead positions from it). Exactly the two formulas it replaces.",
        "plan_owned": "`AttentionOp::history()` = of_span(op.span, op.window). LayerKvGeometry gains `history: HistoryRange`, set from the plan op in plan_continuation_geometry (ConvQkv and any other KV side: Full), so the authority reaches providers (B needs it in V4). `window` stays a geometry fact, informational as before.",
        "backends": "reference, production and device take their floor from `call.history().required_range(position).start`, where `AttentionCall::history()` = of_span(call.span, call.window) — the call's span and window are copied from the plan op at the one construction site (exec/mod.rs). production's `source_start` becomes a thin call to it (device reuses it); reference's local match is deleted.",
        "why_a_field_on_geometry": "`LayerKvGeometry.window` is copied from op.window regardless of span (exec/continuation.rs:260). A provider that read `window` alone would drop rows on a Full or Windowed layer carrying Some(w). The span-aware authority must travel with the geometry.",
        "not_changed": "larql-compute's `window_start` (V2 GQA) and the Metal lowering's window: different paths, outside VIEW-1's backends"
      },
      "what_landed": [
        "exec/kv.rs: `HistoryRange { Full, Trailing(usize) }`, `of_span` (the one exhaustive policy match), `required_start` / `required_range`; `AttentionOp::history()`; `LayerKvGeometry.history` plus `LayerKvGeometry::required_range`; `window` re-documented as a geometry fact, not a retention permission",
        "exec/continuation.rs: plan_continuation_geometry sets `history` from the op (softmax) and `Full` (conv-QKV)",
        "exec/backend.rs: `AttentionCall::history()`",
        "exec/production.rs: `source_start` reads the authority (device reuses it); exec/reference.rs: its local match deleted. Neither backend imports AttentionSpan any more.",
        "28 test literals of LayerKvGeometry gain `history` (None → Full, Some(w) → Trailing(w), the sliding reading those tests intend)"
      ],
      "witnesses": {
        "exec/tests/history_range.rs (6)": "the authority equals the replaced formula for 3 spans × 5 windows × 2,100 positions; only a bounded sliding span trails; plan geometry's history equals op.history() on the sliding fixture (exactly one Trailing(3)); S2 scan clean on reference/production/device; the scan sees a seeded derivation and ignores a comment; the two floor-computing backends read `.history()`",
        "mutation_control": "re-inserting the old match into reference.rs turned both S2 tests red; reverted and re-verified green"
      },
      "falsifiers": {
        "S2": "CLOSED — no backend derives a floor from span or window; one semantic authority",
        "behaviour": "neutral — 9,598 tests across larql-vindex, -kv, -inference, -cli pass with --no-fail-fast, the MEM-1 harness's row/canonical bit-identical parity among them"
      },
      "findings": [
        "During V1 the full gate exposed that #547 had turned main's C3 name ban red (DenseFfnSession built RowKvState by Default — a PLUGIN-1 F2 regression). A fix was prepared here but not pushed: #553 (another session) had already merged the same repair. Recorded because V1's first gate run was not clean for a reason outside V1.",
        "An executor error in V1: a scripted edit of test literals truncated crates/larql-kv/src/vindex3/tests/mod.rs (550 → 33 lines); caught from the diff stat before any build, restored from HEAD, redone (+15/−1)."
      ],
      "gates": {
        "cargo fmt --all --check": "clean",
        "clippy (vindex all-targets; kv; inference; cli; server) -D warnings": "clean",
        "tests": "9,598 passed, 0 failed (--no-fail-fast); server continuation targets pass"
      }
    },
    "V2": {
      "name": "the view",
      "status": "design recorded before code",
      "base": "main a010739c (V1 merged #556; the frozen code facts re-verified here: 6 absolute index sites, 10 provider implementors; #563 touched no attention/KV surface)",
      "representation": {
        "choice": "a trait object behind a Copy struct: `KvView<'a> { base, end, keys: &'a (dyn KvRows + Sync), values: &'a (dyn KvRows + Sync) }` over the one-method trait `KvRows { fn row(&self, position: usize) -> &[f32] }`, where `position` is ABSOLUTE. New module exec/kv_view.rs.",
        "why_not_an_enum": "a closed enum (Rows | Contiguous | …) would make every future backing — paged, codec-backed, an external plugin's own layout — a change to core's enum. The freeze asks for a general view, not one shaped by today's two providers. The trait object keeps it open.",
        "why_not_generic": "a generic parameter on AttentionStepCall would propagate through PlanBackend (object-safe today, held as &dyn and Arc<dyn>); a trait object keeps the backend trait object-safe.",
        "cost_and_its_measurement": "one virtual call per row fetch, in the same place the kernels already call `key_of(p)`/`value_of(p)` closures per position. Measured, not assumed: decode ms/token on qwen3-0.6b (ProductionBackend, 512-token prefill, 64 decode steps), main vs V2, alternating, 3 repeats each; recorded here before V2 merges.",
        "Sync": "the kernels' closures may be called from worker threads, so the rows are `dyn KvRows + Sync`"
      },
      "refusal": {
        "type": "`ViewRefusal { needed: Range<usize>, base, end }` — names the positions the plan requires and what the view holds; converts to VindexError",
        "unbypassable": "AttentionStepCall's view becomes PRIVATE; the only constructor, `AttentionStepCall::new(op, position, rows)`, checks `op.history().required_range(position)` (its history part, start..position) against [base, end) and that end == position. A step whose view drops a required row cannot be built, so no backend ever receives one (S3 on every backend by construction). The view's own key(p)/value(p) assert base <= p < end with a named message; after the constructor's check that is unreachable, and it is an executor bug, not a retention one, if ever hit.",
        "conv_qkv": "its history requirement is Full; the executor checks covers(0..base) before the operator, the same refusal"
      },
      "migration": {
        "index_sites": "the 6 `step.keys[p]` / `step.values[p]` (reference, production, device) become `rows.key(p)` / `rows.value(p)`; `p == position` still returns the fresh row",
        "conv_qkv": "`layer_forward_with(past: KvView, base, …)`; `key_at` / `value_at` / `visible` read past.end() instead of past_keys.len()",
        "providers_unchanged": "the trait keeps keys()/values() in V2; the executor wraps them with `KvView::over_rows` (base 0). The provider trait and ABI change in V3.",
        "conv_qkv_copy_kept": "the executor still copies ConvQkv's rows before taking &mut recurrent_state (behaviour-neutral); Q1 re-measures after V2 and again after V3",
        "construction_sites": "2 production (exec/mod.rs attention_into_kv, exec/decode.rs) and 4 test sites move to AttentionStepCall::new"
      },
      "split_borrow_not_precluded": "KvView borrows `&'a dyn KvRows`; a later provider method returning (KvView<'_>, &mut RecurrentState) from disjoint fields is expressible — nothing in V2 ties the view's borrow to the whole provider",
      "witnesses_planned": {
        "S1": "source scan: no `step.keys[` / `step.values[` / indexing of provider row slices outside kv_view.rs, with a seeded control",
        "S3": "a view whose base exceeds the required start is refused by AttentionStepCall::new, named; a view whose end != position likewise; exercised through each backend's step path",
        "view_invariants": "base <= end enforced at construction; key/value refuse outside [base, end)",
        "behaviour": "full suites --no-fail-fast; MEM-1 harness parity; the C5/C6 journeys",
        "cost": "the qwen decode A/B above"
      }
    }
  }
}
